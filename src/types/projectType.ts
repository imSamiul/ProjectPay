export type ProjectType = {
  name: string;
  budget: number | string;
  advance: number | string;
  due: number | string;
  client: string;
  clientPhone: string;
  clientEmail: string;

  startDate: string;
  endDate: string;

  description: string;
};
