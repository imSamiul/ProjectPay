export type SignUpForm = {
  name: string;
  email: string;
  password: string;
  role: string;
};
export type LoginForm = {
  email: string;
  password: string;
};

export type AddOtherInfoCredentials = {
  role: string;
};
